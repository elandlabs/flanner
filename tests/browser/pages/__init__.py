"""One small class per screen, so a journey reads as steps.

Selectors live here and only here: role and accessible name first, visible
text second. No CSS class selectors — classes are styling, and this app
restyles often.
"""
